import { RevealText, Section } from '.';

export default function Works() {
  return (
    <Section id="works">
      <RevealText id="worksHeading">
        <h2 className="heading-2">My Works</h2>
      </RevealText>
    </Section>
  );
}
